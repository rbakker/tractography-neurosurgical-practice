<nav class="fixed top-0 inset-x-0 z-40 bg-slate-950/80 backdrop-blur border-b border-slate-800">
  <!-- Header row -->
  <div class="max-w-6xl mx-auto px-4 py-3 flex items-center justify-between">
    <a href={`${import.meta.env.BASE_URL}/`} class="font-semibold tracking-tight text-lg">
      Connectome<span class="text-primary">Lab</span>
    </a>
    <button id="menu-btn" class="md:hidden text-slate-200 hover:text-primary focus:outline-none" aria-label="Toggle menu">
      <svg class="w-7 h-7" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" />
      </svg>
    </button>
    <div class="hidden md:flex gap-6 text-sm">
      <a href={`${import.meta.env.BASE_URL}/project`}>Project</a>
      <a href={`${import.meta.env.BASE_URL}/publications`}>Publications</a>
      <a href={`${import.meta.env.BASE_URL}/software`}>Software</a>
      <a href={`${import.meta.env.BASE_URL}/contact`}>Consortium</a>
    </div>
  </div>

  <!-- Mobile dropdown -->
  <div id="mobile-menu" class="max-h-0 overflow-hidden transition-all duration-300 md:hidden border-slate-800">
    <div class="flex flex-col items-end gap-4 px-4 py-4 text-sm border-t border-slate-800">
      <a href={`${import.meta.env.BASE_URL}/project`}>Project</a>
      <a href={`${import.meta.env.BASE_URL}/publications`}>Publications</a>
      <a href={`${import.meta.env.BASE_URL}/software`}>Software</a>
      <a href={`${import.meta.env.BASE_URL}/contact`}>Consortium</a>
    </div>
  </div>
</nav>

<script>
  const btn  = document.getElementById("menu-btn")!;
  const menu = document.getElementById("mobile-menu")!;
  btn.addEventListener("click", () => {
    menu.style.maxHeight = menu.style.maxHeight ? "" : menu.scrollHeight + "px";
  });
</script>
